#include "main.h"

/**
 * main - test that the function prints if integer is positive or negative
 * Return: 0 (Success)
 */

int main(void)
{
	int i;

	i = 98;
	positive_or_negative(i);

	return (0);
}
